apiVersion: apps/v1
kind: Deployment

metadata:
  name: backend-deployment
  namespace: krishibandhu
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: adarsh5559/krishibandhu-backend:8
          imagePullPolicy: Always

          ports:
            - containerPort: 5000

          envFrom:
            - configMapRef: 
                name: backend-config
            - secretRef:
                name: backend-secret  
          
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "512Mi"
              cpu: "500m"
